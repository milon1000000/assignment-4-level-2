enum ActiveStatus {
  ACTIVE
  SUSPENDED
}

enum Role {
  CUSTOMER
  PROVIDER
  ADMIN
}

enum RentalStatus {
  PENDING
  CONFIRMED
  COMPLETED
  CANCELLED
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
}

enum PaymentMethod {
  STRIPE
  SSLCOMMERZ
}